# CI/CD Pipeline для SimpleTask API (GitHub Actions)

> **Описание проекта:** Данный CI/CD pipeline создан для автоматизации процессов проверки, сборки и деплоя учебного Node.js‑приложения **SimpleTask API**. Workflow обеспечивает стабильное качество кода, автоматическое тестирование и подготовку к дальнейшей публикации.

---

## 🎯 Цели Pipeline

1. Проверять код на ошибки и несоответствия стилю.
2. Автоматически запускать тесты после каждого изменения.
3. Собирать приложение после успешного прохождения всех проверок.
4. Подготавливать процесс деплоя (при необходимости — на сервер или в контейнер).

---

## 🔧 Этапы Pipeline

* **Lint** — проверка качества кода.
* **Test** — выполнение модульных тестов.
* **Build** — сборка проекта.
* **Deploy** — шаг для развёртывания (опционально подключаемый).

---

## 📊 Таблица шагов и триггеров

| Этап       | Описание                                        | Триггеры                   |
| ---------- | ----------------------------------------------- | -------------------------- |
| **Lint**   | Проверка стиля и синтаксиса ESLint              | push, pull_request         |
| **Test**   | Запуск всех тестов через Jest                   | push, pull_request         |
| **Build**  | Подготовка сборки приложения                    | push в main                |
| **Deploy** | Выполнение деплоя (например, на VPS или Render) | workflow_dispatch, release |

---

## ⚙️ Пример файла workflow

```yaml
name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Run ESLint
        run: npm run lint

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm test

  build:
    needs: [lint, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm install
      - name: Build project
        run: npm run build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - name: Deploy script placeholder
        run: echo "Deploying application..."  # Здесь может быть SSH, Docker, rsync и т.д.
```

---

## 🔐 Добавление секретов

Чтобы pipeline мог выполнять деплой или подключаться к внешним сервисам:

1. Перейдите в репозиторий на GitHub.
2. Откройте **Settings**.
3. Перейдите в раздел **Secrets and variables → Actions**.
4. Нажмите **New repository secret**.
5. Добавьте необходимые переменные, например:

   * `SERVER_HOST`
   * `SERVER_USER`
   * `SERVER_KEY`

---

## 🚀 Активация и запуск workflow

* Убедитесь, что файл workflow расположен в директории:
  **`.github/workflows/main.yml`**.
* Внесите изменения и выполните `push` в ветку **main**.
* Перейдите во вкладку **Actions**, чтобы увидеть выполнение pipeline.
* Чтобы запустить деплой вручную:
  выберите workflow → **Run workflow**.

---

## 🔗 Полезные ссылки

* [GitHub Actions Documentation](https://docs.github.com/actions)
* [Примеры workflow](https://github.com/actions/starter-workflows)
* [Node.js Documentation](https://nodejs.org)

---

**Выделение текста:** CI/CD помогает избегать ошибок и *ускоряет* разработку.

> «Этот pipeline служит основой для автоматизации SimpleTask API и подходит для учебных проектов.»
